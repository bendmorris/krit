// generated by asset_registry.py
#ifndef KRIT_ASSETS
#define KRIT_ASSETS

namespace krit {

enum AssetId: int {
    NoAsset = -1,
    {% for asset in assets: -%}
    {{ asset.id }} = {{ loop.index - 1 }},
    {% endfor -%}
    AssetCount,
};

enum AssetRoot: int {
    {% for root in roots: -%}
    {{ root.id }} = {{ loop.index - 1 }},
    {% endfor -%}
    AssetRootCount,
};

}

#endif
