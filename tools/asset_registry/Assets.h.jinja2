// generated by asset_registry.py
#ifndef KRIT_ASSETID
#define KRIT_ASSETID

namespace krit {

enum AssetId: int {
    NoAsset = -1,
    {% for asset in assets: -%}
    {{ asset.id }} = {{ loop.index - 1 }},
    {% endfor -%}
    AssetCount,
};

enum AssetRoot: int {
    {% for root in roots: -%}
    {{ root.id }} = {{ loop.index - 1 }},
    {% endfor -%}
    AssetRootCount,
};

}

#endif
