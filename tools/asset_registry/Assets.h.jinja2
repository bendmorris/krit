// generated by asset_registry.py
#ifndef KRIT_ASSETS
#define KRIT_ASSETS

#include "krit/asset/AssetInfo.h"

namespace krit {

enum AssetId {
    {% for asset in assets: -%}
    {{ asset.assetId }} = {{ loop.index - 1 }},
    {% endfor -%}
    AssetCount,
};

struct Assets {
    static const AssetInfo assets[AssetCount];
};

}

#endif
