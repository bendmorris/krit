#ifndef KRIT_ASSET_ASSETTYPE
#define KRIT_ASSET_ASSETTYPE

namespace krit {

enum AssetType {
    ImageAsset,
    AtlasAsset,
    FontAsset,
    TextAsset,
    SpineSkeletonAsset,
    AudioAsset,
    ParticleAsset,
    AssetTypeCount,
};

}

#endif
